{
  "name": "@gappris/api",
  "version": "1.0.0",
  "contributors": [],
  "private": true,
  "license": "MIT",
  "engines": {
    "node": "16"
  },
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint --ext .js,.ts .",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "serve": "yarn build && firebase emulators:start --import=exportDir --only functions,auth,firestore,storage",
    "setMockData": "sleep 18 && echo '\ncalling curl set mock data cmd 😉 \n' && curl http://localhost:5001/gapris-6b7d5/us-central1/setMockData && echo '\ncalled curl mock cmd! 😘 \n' ",
    "serve:test-data": "concurrently \"yarn serve\" \"yarn setMockData\"",
    "shell": "yarn build && firebase functions:shell",
    "start": "yarn shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log",
    "export": "firebase emulators:export exportDir"
  },
  "dependencies": {
    "firebase-admin": "^10.0.2",
    "firebase-functions": "^3.18.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^8.3.4",
    "@typescript-eslint/eslint-plugin": "^5.12.0",
    "@typescript-eslint/parser": "^5.3.1",
    "concurrently": "^7.5.0",
    "eslint": "^8.24.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-import": "^2.26.0",
    "firebase-functions-test": "^0.2.0",
    "typescript": "~4.8.4"
  },
  "fireConfig": {}
}
